{
  "name": "drone-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
{
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://webappassignmentbackend.onrender.com/:path*" }
  ]
}
